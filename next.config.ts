import { i18n } from './next-i18next.config';

const nextConfig = {
  i18n,
  images: {
    domains: ['template-03-api.vercel.app', 'randomuser.me', 'fakestoreapi.com'],
  },
};

export default nextConfig;
